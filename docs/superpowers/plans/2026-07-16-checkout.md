# Checkout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the missing production-ready `/checkout` page with deterministic guards, required legal acknowledgement, and the existing local purchase transaction.

**Architecture:** A pure checkout eligibility module drives a dedicated client screen. The cart routes to checkout instead of purchasing, while the marketplace provider remains the single source of truth for cart, balance, session, orders, and transactions.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript, CSS Modules, Node test runner.

## Global Constraints

- Work only in `/Users/anastasiavolkova/Documents/Vault/concept-skins`.
- Do not use Git or GitHub.
- Do not install dependencies.
- Preserve the approved Home visual system.
- Use only mock/local transactions; do not add APIs or payment providers.
- Follow TDD: test must fail before implementation.

---

### Task 1: Checkout eligibility model

**Files:**
- Create: `src/lib/checkout.test.ts`
- Create: `src/lib/checkout.ts`

**Interfaces:**
- Produces: `getCheckoutGate(input): CheckoutGate`
- Produces: `canSubmitCheckout(gate, accepted): boolean`

- [ ] **Step 1: Write failing tests** for empty, insufficient, auth-required, Steam-required, ready, and checkbox-gated submission.
- [ ] **Step 2: Run** `node --test src/lib/checkout.test.ts` and confirm missing-module failure.
- [ ] **Step 3: Implement** a pure priority-ordered gate: `empty → insufficient → auth-required → steam-required → ready`.
- [ ] **Step 4: Re-run** the focused test and confirm all assertions pass.

### Task 2: Safe authentication return

**Files:**
- Modify: `src/lib/auth.test.ts`
- Modify: `src/lib/auth.ts`

**Interfaces:**
- Extends: `AllowedAuthPath` with `/checkout`.

- [ ] **Step 1: Add a failing assertion** that `sanitizeAuthReturnPath('/checkout')` returns `/checkout`.
- [ ] **Step 2: Run** `node --test src/lib/auth.test.ts` and confirm it fails.
- [ ] **Step 3: Add `/checkout`** to the union and allowlist.
- [ ] **Step 4: Re-run** auth tests and confirm pass.

### Task 3: Checkout screen and route states

**Files:**
- Create: `src/features/checkout/CheckoutScreen.tsx`
- Create: `src/features/checkout/checkout.module.css`
- Create: `src/app/checkout/page.tsx`
- Create: `src/app/checkout/loading.tsx`
- Create: `src/app/checkout/error.tsx`

**Interfaces:**
- Consumes: marketplace provider state and `checkoutCart()`.
- Consumes: `getCheckoutGate()` and `canSubmitCheckout()`.
- Produces: `/checkout` with all loading, guard, ready, submitting, error, and success states.

- [ ] **Step 1: Build semantic sections** for breadcrumbs, order products, fulfillment, and sticky summary.
- [ ] **Step 2: Add the exact acknowledgement**: `Я принимаю условия Пользовательского соглашения и согласен с Политикой конфиденциальности.` with both document names linked.
- [ ] **Step 3: Keep purchase disabled** until acknowledgement and ready gate are both true.
- [ ] **Step 4: Call `checkoutCart()` once**, render the local receipt, and link to `/account/purchases` and `/catalog`.
- [ ] **Step 5: Add route loading/error boundaries** using existing UI primitives.

### Task 4: Cart and top-up integration

**Files:**
- Modify: `src/features/cart/CartScreen.tsx`
- Modify: `src/features/top-up/TopUpScreen.tsx`

**Interfaces:**
- Cart ready CTA produces navigation to `/checkout`.
- Top-up checkbox uses the exact text required by the later specification.

- [ ] **Step 1: Remove direct purchase mutation** from CartScreen and replace the ready button with `Перейти к оформлению` linking to `/checkout`.
- [ ] **Step 2: Preserve existing insufficient balance and auth recovery links** unchanged.
- [ ] **Step 3: Restore the exact top-up acknowledgement copy** with links to terms and privacy.
- [ ] **Step 4: Run typecheck and lint** and remove only imports/state made obsolete by the cart change.

### Task 5: Design Review and QA

**Files:**
- Modify only files with verified High or Medium issues.

- [ ] **Step 1: Run** `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build`.
- [ ] **Step 2: Verify** `/cart → /checkout → success` and every guard state in the browser.
- [ ] **Step 3: Check** 1280px and 390px layouts, overflow, focus, disabled state, loading, error, empty, links, and console.
- [ ] **Step 4: Fix all High and Medium findings**, then repeat the full verification.
- [ ] **Step 5: Stop** after the final report; do not begin unrelated pages.

## Self-review

- Full Checkout requirement is covered.
- Required exact legal copy is covered in Checkout and top-up.
- No separate payment provider or invented method is introduced.
- Types and function names are consistent across tasks.
- No commit steps are included because the user prohibits Git.
